# Server environment variables
# Copy this file to .env and adjust values for your local setup

# Server
PORT=5003
NODE_ENV=development
JWT_SECRET=change_this_to_a_long_random_string
CLIENT_URL=http://localhost:5173

# PostgreSQL (used when DATABASE_URL is not provided)
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=faculty_reporting

# Alternatively, use a full connection string (overrides the above)
# DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/faculty_reporting

# Uploads
UPLOAD_PATH=./uploads
MAX_FILE_SIZE=10485760
